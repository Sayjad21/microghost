# Public Assets

Place your final MicroGhost logo at:

```text
frontend/public/logo.png
```

The frontend automatically uses `/logo.png` for the header mark when that file exists.
If it is missing, the app shows the built-in MicroGhost fallback mark.
